# Clone-install superset (core + all optional backends). Lower-bound floors;
# see requirements.lock for the exact pinned closure used in CI.
# The minimal install needs only the core five (the synthetic backend needs
# neither torch nor transformers).
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
plotly>=5.18
streamlit>=1.30
torch>=2.1
transformers>=4.44
umap-learn>=0.5
faiss-cpu>=1.7
pytest>=7.4
